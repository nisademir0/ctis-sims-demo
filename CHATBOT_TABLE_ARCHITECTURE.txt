# Type-Aware Interactive Tables - Architecture & Flow

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          USER INTERACTION FLOW                              │
└─────────────────────────────────────────────────────────────────────────────┘

  User Types Query
        ↓
  ┌─────────────────┐
  │   Frontend      │
  │  ChatbotPage    │
  └────────┬────────┘
           │ POST /api/chatbot/ask
           ↓
  ┌─────────────────────────────────────────────────────────────────────────┐
  │                         Backend (Laravel)                               │
  │  ┌─────────────────────────────────────────────────────────────────┐   │
  │  │              ChatbotController::ask()                           │   │
  │  │                                                                  │   │
  │  │  1. Validate & sanitize query                                   │   │
  │  │  2. Forward to AI Service (Python)                              │   │
  │  │  3. Receive SQL + results                                       │   │
  │  │  4. ┌──────────────────────────────────────────────────────┐   │   │
  │  │     │   detectColumnTypes($results)                        │   │   │
  │  │     │                                                       │   │   │
  │  │     │   For each column:                                   │   │   │
  │  │     │   ┌─────────────────────────────────────────────┐   │   │   │
  │  │     │   │  Collect unique values                      │   │   │   │
  │  │     │   │  Sample 10 values (80% threshold)           │   │   │   │
  │  │     │   │                                              │   │   │   │
  │  │     │   │  ┌─────────────────────────────┐            │   │   │   │
  │  │     │   │  │  isDateColumn()?            │ YES → date │   │   │   │
  │  │     │   │  │  (Carbon::parse validation) │            │   │   │   │
  │  │     │   │  └─────────────┬───────────────┘            │   │   │   │
  │  │     │   │                │ NO                          │   │   │   │
  │  │     │   │  ┌─────────────▼───────────────┐            │   │   │   │
  │  │     │   │  │  isNumericColumn()?         │ YES → number│  │   │   │
  │  │     │   │  │  (is_numeric check)         │            │   │   │   │
  │  │     │   │  └─────────────┬───────────────┘            │   │   │   │
  │  │     │   │                │ NO                          │   │   │   │
  │  │     │   │  ┌─────────────▼───────────────┐            │   │   │   │
  │  │     │   │  │  unique_values ≤ 20?        │ YES → categorical│ │   │
  │  │     │   │  │  (cardinality check)        │            │   │   │   │
  │  │     │   │  └─────────────┬───────────────┘            │   │   │   │
  │  │     │   │                │ NO                          │   │   │   │
  │  │     │   │                └─────────────────→ text      │   │   │   │
  │  │     │   └─────────────────────────────────────────────┘   │   │   │
  │  │     └──────────────────────────────────────────────────────┘   │   │
  │  │                                                                  │   │
  │  │  5. Build response:                                             │   │
  │  │     {                                                            │   │
  │  │       "response": "Sorgunuz için X sonuç bulundu.",             │   │
  │  │       "tables": [                                               │   │
  │  │         {                                                        │   │
  │  │           "headers": [...],                                     │   │
  │  │           "rows": [...],                                        │   │
  │  │           "total_count": X,                                     │   │
  │  │           "column_types": {                                     │   │
  │  │             "name": {"type": "text", "unique_values": null},    │   │
  │  │             "category": {                                       │   │
  │  │               "type": "categorical",                            │   │
  │  │               "unique_values": ["A", "B", "C"]                  │   │
  │  │             },                                                   │   │
  │  │             "quantity": {"type": "number", ...},                │   │
  │  │             "created_at": {"type": "date", ...}                 │   │
  │  │           }                                                      │   │
  │  │         }                                                        │   │
  │  │       ],                                                         │   │
  │  │       "query_metadata": {                                       │   │
  │  │         "sql_query": "SELECT ...",                              │   │
  │  │         "has_tables": true                                      │   │
  │  │       }                                                          │   │
  │  │     }                                                            │   │
  │  └─────────────────────────────────────────────────────────────────┘   │
  └────────────────────────────────┬────────────────────────────────────────┘
                                   │ JSON Response
                                   ↓
  ┌────────────────────────────────────────────────────────────────────────┐
  │                      Frontend (React)                                  │
  │  ┌─────────────────────────────────────────────────────────────────┐  │
  │  │                    ChatbotPage.jsx                              │  │
  │  │                                                                  │  │
  │  │  Parse response → message.tables.map(table => ...)             │  │
  │  └──────────────────────────────┬──────────────────────────────────┘  │
  │                                 │                                      │
  │  ┌──────────────────────────────▼──────────────────────────────────┐  │
  │  │              InteractiveTable Component                         │  │
  │  │                                                                  │  │
  │  │  Props:                                                          │  │
  │  │    - headers: ["id", "name", "category", "created_at", ...]    │  │
  │  │    - rows: [[1, "Laptop", "Electronics", "2024-12-06"], ...]   │  │
  │  │    - columnTypes: {                                             │  │
  │  │        "category": {                                            │  │
  │  │          "type": "categorical",                                 │  │
  │  │          "unique_values": ["Electronics", "Furniture"]          │  │
  │  │        },                                                        │  │
  │  │        "created_at": {"type": "date", ...}                      │  │
  │  │      }                                                           │  │
  │  │                                                                  │  │
  │  │  ┌────────────────────────────────────────────────────────────┐ │  │
  │  │  │          Render Table Headers                              │ │  │
  │  │  │                                                             │ │  │
  │  │  │  For each header:                                          │ │  │
  │  │  │    1. Sort button                                          │ │  │
  │  │  │    2. Icon (if date: 📅)                                   │ │  │
  │  │  │    3. ┌──────────────────────────────────────────────┐    │ │  │
  │  │  │       │  Type-Aware Filter Input                     │    │ │  │
  │  │  │       │                                               │    │ │  │
  │  │  │       │  if (type === 'categorical'):                │    │ │  │
  │  │  │       │    <select>                                  │    │ │  │
  │  │  │       │      <option>Tümü</option>                   │    │ │  │
  │  │  │       │      {unique_values.map(v => <option>)}      │    │ │  │
  │  │  │       │    </select>                                 │    │ │  │
  │  │  │       │                                               │    │ │  │
  │  │  │       │  else if (type === 'number'):                │    │ │  │
  │  │  │       │    <input type="number" />                   │    │ │  │
  │  │  │       │                                               │    │ │  │
  │  │  │       │  else if (type === 'date'):                  │    │ │  │
  │  │  │       │    <input type="text"                        │    │ │  │
  │  │  │       │           placeholder="Tarih ara..." />      │    │ │  │
  │  │  │       │                                               │    │ │  │
  │  │  │       │  else:                                        │    │ │  │
  │  │  │       │    <input type="text"                        │    │ │  │
  │  │  │       │           placeholder="Filtrele..." />       │    │ │  │
  │  │  │       └──────────────────────────────────────────────┘    │ │  │
  │  │  └────────────────────────────────────────────────────────────┘ │  │
  │  │                                                                  │  │
  │  │  ┌────────────────────────────────────────────────────────────┐ │  │
  │  │  │          Filter Logic (useMemo)                            │ │  │
  │  │  │                                                             │ │  │
  │  │  │  1. Global text filter (search all cells)                 │ │  │
  │  │  │  2. Column-specific filters:                              │ │  │
  │  │  │                                                             │ │  │
  │  │  │     if (type === 'categorical'):                          │ │  │
  │  │  │       ✓ Exact match                                       │ │  │
  │  │  │       String(row[col]) === filterValue                    │ │  │
  │  │  │                                                             │ │  │
  │  │  │     else if (type === 'number'):                          │ │  │
  │  │  │       ✓ Numeric equality                                  │ │  │
  │  │  │       parseFloat(row[col]) === parseFloat(filterValue)    │ │  │
  │  │  │                                                             │ │  │
  │  │  │     else if (type === 'date'):                            │ │  │
  │  │  │       ✓ Search in formatted date                          │ │  │
  │  │  │       formatDate(row[col]).includes(filterValue)          │ │  │
  │  │  │                                                             │ │  │
  │  │  │     else:                                                  │ │  │
  │  │  │       ✓ Contains search (case-insensitive)                │ │  │
  │  │  │       String(row[col]).toLowerCase().includes(...)        │ │  │
  │  │  │                                                             │ │  │
  │  │  │  3. Sort (if sortColumn set)                              │ │  │
  │  │  └────────────────────────────────────────────────────────────┘ │  │
  │  │                                                                  │  │
  │  │  ┌────────────────────────────────────────────────────────────┐ │  │
  │  │  │          Render Table Rows                                 │ │  │
  │  │  │                                                             │ │  │
  │  │  │  filteredAndSortedRows.map(row => {                       │ │  │
  │  │  │    row.map((cell, idx) => {                               │ │  │
  │  │  │      const header = headers[idx];                         │ │  │
  │  │  │      const type = columnTypes[header]?.type;              │ │  │
  │  │  │                                                             │ │  │
  │  │  │      if (type === 'date') {                               │ │  │
  │  │  │        display = formatDate(cell);  // "06.12.2024 14:30"│ │  │
  │  │  │      } else {                                              │ │  │
  │  │  │        display = cell;                                    │ │  │
  │  │  │      }                                                     │ │  │
  │  │  │                                                             │ │  │
  │  │  │      return <td>{display}</td>                            │ │  │
  │  │  │    })                                                      │ │  │
  │  │  │  })                                                        │ │  │
  │  │  └────────────────────────────────────────────────────────────┘ │  │
  │  └──────────────────────────────────────────────────────────────────┘  │
  └────────────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                         COLUMN TYPE EXAMPLES                                │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ CATEGORICAL                                                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│ Detection: ≤20 unique values, count(rows) > 10                             │
│ UI: ┌─────────────────┐                                                    │
│     │ Tümü          ▼ │  ← Dropdown                                        │
│     └─────────────────┘                                                    │
│     • Electronics                                                           │
│     • Furniture                                                             │
│     • Supplies                                                              │
│ Filter: Exact match (String === String)                                    │
│ Examples: category, status, type, department, grade                        │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ NUMBER                                                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│ Detection: is_numeric(), 80% of samples numeric                            │
│ UI: ┌─────────────────┐                                                    │
│     │      [5]        │  ← Number input                                    │
│     └─────────────────┘                                                    │
│ Filter: Numeric equality (parseFloat === parseFloat)                       │
│ Examples: id, quantity, price, age, count                                  │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ DATE                                                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│ Detection: Carbon::parse() success, 80% of samples valid dates             │
│ Display: 06.12.2024 14:30  ← Turkish format                                │
│ Header: name 📅  ← Calendar icon                                           │
│ UI: ┌─────────────────┐                                                    │
│     │ Tarih ara...    │  ← Text input (searches formatted date)           │
│     └─────────────────┘                                                    │
│ Filter: Search in formatted string ("06.12" matches "06.12.2024 14:30")   │
│ Examples: created_at, updated_at, date, timestamp                          │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ TEXT                                                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│ Detection: Default (anything else)                                         │
│ UI: ┌─────────────────┐                                                    │
│     │ Filtrele...     │  ← Text input                                      │
│     └─────────────────┘                                                    │
│ Filter: Contains search, case-insensitive                                  │
│ Examples: name, description, notes, location                               │
└─────────────────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                            KEY BENEFITS                                     │
└─────────────────────────────────────────────────────────────────────────────┘

✅ USER EXPERIENCE
  • Intuitive: Right input type for each column
  • Fast: Dropdown selection vs. typing
  • Clear: Visual indicators (icons, input types)
  • Flexible: Each type has optimal filtering

✅ PERFORMANCE
  • Backend: One-time type detection
  • Frontend: Memoized filtering
  • Smart: Sample-based validation (not full scan)

✅ MAINTAINABILITY
  • Separation: Backend structure, frontend presentation
  • Extensible: Easy to add new types
  • Type-safe: Validation at each step

✅ ACCURACY
  • Categorical: Exact match (no typos)
  • Number: Numeric equality (not string)
  • Date: Format-aware search
  • Text: Flexible contains search
